import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nikitayushchenko.swiftdocshub',
  appName: 'Swift Docs Hub',
  webDir: 'dist'
};

export default config;
